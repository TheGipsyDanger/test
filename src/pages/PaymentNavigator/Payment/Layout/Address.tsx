import * as React from "react";

import { Wrapped, Icon } from "~/components";

export const Address = () => {
  return (
    <Wrapped>
      <Icon name="map-marker-alt" lib="FontAwesome5" color="white" size={20} />
    </Wrapped>
  );
};
