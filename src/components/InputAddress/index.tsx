import * as React from 'react';

import { IInputAddress } from '~/components/InputAddress/data';
import { InputAddress as Layout } from './Layout';

export const InputAddress = (props: IInputAddress ) => <Layout {...props} />;