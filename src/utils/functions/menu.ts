import { IMenuCategory } from '..';

export function populateProducts(data: IMenuCategory[]) {
  // return data.map((item: IMenuCategory) => ({
  //   ...item,
  //   products: item.products.map((product) => ({
  //     ...product,
  //     productStore: {
  //       ...product,
  //       price: BRLMoney(product.productStore?.price as number),
  //     },
  //   })),
  // }));
}
